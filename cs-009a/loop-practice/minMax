#7) Problem (min/max) : Write a function called minMax() that continually asks the user to
#enter an integer until they type Quit. Once the user quits, the program should output the
#smallest (min) and the largest (max) number the user entered. Hint use a while loop
#Sample Output (User input is shown in orange)
#Enter a number, or enter QUIT to stop: 6
#Enter a number, or enter QUIT to stop: 10
#Enter a number, or enter QUIT to stop: -1
#Enter a number, or enter QUIT to stop: Quit
#The MIN number entered is -1 and the MAX number entered is 10 .
def minMax():
    userInput = input("Enter a number, or enter QUIT to stop.")
    #int(userInput).lower() #Cast
    numList = []
    
    while userInput != "QUIT":
        userInput = input("Enter a number, or enter QUIT to stop.")
        numList.append(int(userInput))
    
    if numList:
        print(f"The MIN number entered is {min(numList)} and the MAX number entered is {max(numList)}.")
    else:
        print("No numbers entered.")
    #print(f"Smallest number: {numList.min()}")
    #print(f"Largest number: {numList.max()}")