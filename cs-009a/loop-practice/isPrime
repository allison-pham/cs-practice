#5) Problem (isPrime) : Write a function called isPrime( num) that takes an integer and prints
#True if its prime and False otherwise.
#EXAMPLE
#isPrime(13) -> True
#isPrime(12) -> False
import math
def isPrime(num): #num is int
    #if prime: return True
    if num <= 1:
        return False
    
    i = 2
    while i <= math.sqrt(num):
        if num % i == 0:
            return False
        i += 1

    return True