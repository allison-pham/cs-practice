#4) Problem (hasVowel) : Write a function called hasVowel(str) that receives a string as an
#argument, and returns True if the str has a vowel (a,e,I,o,u) and False otherwise.
#EXAMPLE
#hasVowel( “FRIENDS”) => returns True
#hasVowel(“TV”) => returns False
def hasVowel(str):
    for i in str:
        if i.lower() in ['a', 'e', 'i', 'o', 'u']:
        #if str[i] == 'a' or str[i] == 'e' or str[i] == 'i' or str[i] == 'o' or str[i] == 'u':
            return True
    return False