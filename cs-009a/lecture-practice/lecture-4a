#Answer for Pizza Menu
'''toppingsList = ['Pepperoni', 'Mushrooms', 'Pineapple', 'Sausage', 'Bell Peppers', 'Salami']
cost = 10.99 # Default of the cost of the pizza with no toppings
userInput = 'y'

while(userInput == 'y'):
    toppingsIndex = 0
    
    while toppingsIndex < len(toppingsList):
        print(f"{toppingsIndex + 1}. {toppingsList[toppingsIndex]}")
        toppingsIndex += 1
        # Test piece-by-piece
    
    userTopping = int(input("Enter the number of the topping you'd like to have."))
    if userTopping <= len(toppingsList):
        cost += 1.50
        toppingsList.pop(userTopping - 1)
    else:
        print('Invalid topping number.')
    
    userInput = input("Enter 'y' to add another topping. Enter anything else to quit")

print(f"The total cost of your pizza is: ${cost}")

cost *= 1.075
print(f"The total with tax is ${cost:.2f}")

cost *= 1.15
print(f"The total after tip: ${cost:.2f}")'''

#Answer for Random Number Generator
'''# Generate a random number between 1 to 20 and ask the useer to guess the number until they get it right
import random # Need to import

mysteryNumber = random.randint(1, 20)
# user_input = int(input("Guess a number."))
userNumber = -1 # Can be any value 

while (userNumber != mysteryNumber):
    userNumber = int(input("Guess a number (1-20)."))
    
    if (userNumber != mysteryNumber):
        print("Incorrect")

print("Correct!")'''