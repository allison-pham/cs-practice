#11) (15 points) Write a function called MiddleGuess(), that takes in 1 integer input guessNum,
#and 1 list of integers numList. If guessNum is equal to the value at the middle of numList,
#return True. Otherwise return false.
def middleGuess(guessNum, numList):
    middleIndex = len(numList) // 2
    middleVal = numList[middleIndex]
    if guessNum == middleVal:
        return True
    return False

my_list = [20, 5, 12, 7]
print(middleGuess(5, my_list))