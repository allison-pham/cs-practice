import chn.util.*;
public class Tester {
    public static void main(String[] args) {
   	 boolean endSim=false;
   	 System.out.println("I can translate English words into Pig Latin.");
   	 while(endSim == false) {
   		 ConsoleIO input=new ConsoleIO();
   		 Piglatinator lab=new Piglatinator();
   		 String ask=input.promptToken("Type a word and then press <Enter>.\n");
   		 String x=lab.Translate(ask);
  		 
   		 String repeat=input.promptToken("Would you like to translate another phrase?\n"); //Outside of loop to ask to repeat for the 1st time
   		 repeat.toLowerCase();

   		 if(repeat.equals("no") || repeat.equals("n")) {
       		 endSim = true;
   		 }

   		 // while(repeat.equals("yes") || repeat.equals("y")) { //while loop to repeatedly ask
   		 // ask=input.promptToken("Type a word and then press <Enter>.\n");
   		 // x=lab.Translate(ask);

   		 // repeat=input.promptToken("Would you like to translate another phrase?\n"); //Outside of loop to ask to repeat for the 1st time
   		 // repeat.toLowerCase();
   		 // }
   	 }
    }
}