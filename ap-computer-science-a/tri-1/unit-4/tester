import chn.util.*;
public class Tester {
    public static void main(String[] args) {
   	 ConsoleIO input=new ConsoleIO();
   	 System.out.println("Welcome to the Palindrome Program! Enter Q or q to quit.");
  	 
   	 System.out.println("Enter a string: ");
   	 String word=input.readToken();
  	 
   	 // String confirm="";
   	 // Palindrome.Check(word);
   	 // confirm=input.readLine();
  	 
   	 Palindrome yes=new Palindrome();
   	 yes.Check(word);
    }
}