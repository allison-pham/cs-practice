# Transfer over to PyCharm

def maxMagnitude(numList):
    # Given (a, b, c), find the max magnitude
    maxVal = -1

    # for i in numList:
        # if negative: multiply by -1
    
    # for k in numList:
        # if currentVal > maxVal: maxVal = currentVal

    return maxVal